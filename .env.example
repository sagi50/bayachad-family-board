# Local: open exactly http://localhost:8080
APP_ORIGIN=http://localhost:8080
INITIAL_USERNAME=SAGI HALILI
INITIAL_DISPLAY_NAME=שגיא
# Production: replace with a DNS name pointing at the EC2 Elastic IP.
DOMAIN=tasks.example.com
