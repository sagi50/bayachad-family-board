FROM python:3.12-slim
ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1
WORKDIR /app
COPY requirements.lock ./
RUN pip install --no-cache-dir -r requirements.lock && useradd --uid 10001 --create-home api
COPY app ./app
COPY migrations ./migrations
COPY alembic.ini ./
USER api
EXPOSE 8000
CMD ["sh", "-c", "alembic upgrade head && python -m app.manage bootstrap && exec uvicorn app.main:app --host 0.0.0.0 --port 8000 --no-access-log --no-proxy-headers"]
